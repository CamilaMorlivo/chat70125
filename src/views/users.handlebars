<div>
    <h1>Bienvenida {{ name }}</h1>
    
    {{#if isAdmin}}
        {{#each users}}
            <div>
                <p>Nombre: {{this.full_name}} - email: {{this.email}}</p>
                <hr>
            </div>
        {{/each}}
    {{else}}
        <h2>No tienes permisos para ver los usuarios.</h2>
    {{/if}}

    <script src='/assets/js/index.js'></script>

</div>